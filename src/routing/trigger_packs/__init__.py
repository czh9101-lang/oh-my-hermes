"""Repo-shipped trigger language packs, one `<lang>.json` per language.

Data only: the schema, the validator, and the merge live in
`omh.routing.trigger_language_packs`. Adding a language means adding a file
here -- see `docs/routing-quality.md`.
"""
