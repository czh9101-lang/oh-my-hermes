"""Core facade for the canonical lexer shipped with the standalone plugin."""

from ..plugin_bundle.omh.reference_regions import (
    ReferenceRegions,
    executable_routing_text,
    reference_regions,
)

__all__ = ["ReferenceRegions", "executable_routing_text", "reference_regions"]
